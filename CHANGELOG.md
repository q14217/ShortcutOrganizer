# 更新日志

本文件记录所有值得注意的项目变更。  
格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## [未发布]

### 计划中
- 从剪贴板粘贴路径快速添加
- 快捷方式失效自动重定位
- 关于对话框
- 备份文件格式版本号

---

## [1.1.0] - 2026-09-17

### 新增
- **单实例限制**：防止多次启动导致数据文件冲突
- **固定到常用**：右键菜单可手动置顶任意快捷方式
- **最近使用面板**：新增"🕐 最近"虚拟分类，显示 7 天内打开的项
- **拖拽归类**：拖拽卡片到左侧分类列表即可完成移动，悬停时目标分类高亮
- **路径变量化**：备份文件保存时用 `%USERPROFILE%`、`%APPDATA%` 等变量替换绝对路径，实现跨机器恢复
- **资源管理器右键集成**：可在设置中开启"发送到奥格快捷收纳箱"，右键任意文件即可快速添加
- **`--add` 命令行模式**：支持 `ShortcutOrganizer.exe --add "文件路径"` 快速添加
- **配置文件监听**：主程序运行时，`data.json` 变化会自动重新加载
- **自动备份**：每天首次启动自动备份数据到 `backups/`，保留 10 天
- **数据损坏自愈**：`data.json` 解析失败时自动从最近备份恢复
- **热键 UI 反馈**：设置面板实时显示热键状态，注册失败时明确提示
- **LRU 图标缓存**：图标缓存上限 500 条，防止长期运行内存增长
- **日志系统**：`Logger` 类写入 `%LocalAppData%\ShortcutOrganizer\logs\`，替代所有 `Debug.WriteLine`
- **首次启动默认分类**：自动创建"聊天通讯 / 游戏 / 影视 / 音乐"等 10 个预设分类
- **快捷键说明窗口**：`F1` 打开，列出所有可用快捷键
- **视图切换**：卡片视图 ↔ 列表视图，快捷键 `Ctrl+L`
- **CSV 导入**：支持从 CSV 文件导入快捷方式
- **设置面板**：集中管理热键、视图、托盘、备份等选项
- **分类拖拽排序**：拖动分类名调整顺序

### 修复
- 修复图标在后台线程创建导致的 `XamlParseException` 异常（`BitmapSource.Freeze()`）
- 修复 `SortComboBox` 初始化时触发 `NullReferenceException`
- 修复从磁盘根目录导入文件夹时的 `UnauthorizedAccessException`
- 修复 JSON 反序列化时 `DateTime` 格式不兼容问题
- 修复深色主题下 `DropShadowEffect` 颜色类型不匹配
- 修复单文件发布后托盘图标变成白色的 bug

### 优化
- 拆分 `MainWindow` 到 12 个 partial class，提升可维护性
- 用 `VirtualizingWrapPanel` 替换普通 `WrapPanel`，1000+ 条数据依然流畅
- `data.json` 保存改为防抖（1200ms），减少磁盘写入
- 移除 WinForms 隐式 using，消除与 WPF 的类型二义性

### 移除
- 移除临时拖拽排序功能（功能不完善，暂不发布）

---

## [1.0.0] - 2026-08-01

### 首发版本

#### 核心功能
- 分类管理（新建 / 重命名 / 删除 / 批量删除）
- 快捷方式添加（`.lnk` 文件 / 任意文件）
- 快捷方式打开（双击 / 右键 / `Enter`）
- 拖放文件到窗口添加
- 搜索与排序
- 状态栏显示

#### 数据管理
- `data.json` 本地存储
- 手动备份 / 恢复
- 导出为 CSV

#### 图标系统
- 从 exe/lnk 提取关联图标
- 图标异步加载
- 目标路径失效标记（红点）

#### 界面
- 深色 / 浅色主题切换
- 卡片式快捷方式展示
- 空状态提示
- 状态栏进度反馈

#### 系统集成
- 全局热键 `Ctrl+Alt+S`
- 系统托盘（最小化 / 双击恢复 / 右键菜单）
- 窗口位置居中

---

## 版本号规则

- **主版本号（MAJOR）**：不兼容的 API 变更
- **次版本号（MINOR）**：向下兼容的功能新增
- **修订号（PATCH）**：向下兼容的问题修正

例如：
- `1.0.0` → `1.0.1`：修了个 bug
- `1.0.1` → `1.1.0`：加了个新功能
- `1.1.0` → `2.0.0`：数据结构不兼容（老备份无法恢复）

---

## 发布流程

每次发布新版本时：

1. 更新本文件：把"未发布"内容移到新的版本号下
2. 更新 `ShortcutOrganizer.csproj` 里的 `<Version>`
3. 打 Git 标签：
   ```bash
   git tag -a v1.1.0 -m "发布 1.1.0"
   git push origin v1.1.0
   ```
4. 用 `publish.bat` 生成发布文件
5. 用 Inno Setup 编译安装包
6. 上传安装包到 GitHub Releases