<Window x:Class="ShortcutOrganizer.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" 
        Height="200" 
        Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Prompt}" Margin="0,0,0,10"/>

        <ComboBox x:Name="CategoryComboBox" 
                  Grid.Row="1"
                  IsEditable="True"
                  Margin="0,0,0,20"
                  ItemsSource="{Binding ExistingCategories}"
                  Text="{Binding InputText}"/>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="确定" Width="80" Margin="0,0,10,0" IsDefault="True" Click="OkButton_Click"/>
            <Button Grid.Column="2" Content="取消" Width="80" IsCancel="True" Click="CancelButton_Click"/>
        </Grid>
    </Grid>
</Window>